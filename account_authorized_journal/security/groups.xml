<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Grupo de limite de diarios -->
        <record id="group_journal_limits" model="res.groups">
            <field name="name">Límites de Diarios</field>
            <field name="category_id" ref="base.module_category_accounting"/>
        </record>

        <!-- Hacer que group_journal_limits se aplique sobre base.group_user -->
        <record id="base.group_user" model="res.groups">
            <field name="implied_ids" eval="[(4, ref('account_authorized_journal.group_journal_limits'))]"/>
        </record>
    </data>
</odoo>